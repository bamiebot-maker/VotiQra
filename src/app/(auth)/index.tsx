import { Text, View } from 'react-native';

export default function AuthPlaceholder(): JSX.Element {
  return (
    <View className="flex-1 items-center justify-center bg-primary-500">
      <Text className="text-xl font-bold text-white">Auth Placeholder</Text>
    </View>
  );
}
